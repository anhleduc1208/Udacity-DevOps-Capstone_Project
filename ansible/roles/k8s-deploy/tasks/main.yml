- name: Copy deployment configuration
  copy:
    src: "app-deployment.yaml"
    dest: $HOME

- name: Apply deployment configuration
  shell: "./bin/kubectl apply -f app-deployment.yaml"
  args:
    chdir: $HOME
